# @package ssl
# Contrastive (SimCLR-style) SSL Configuration
#
# Timestep-level tokenization: two timestep masks create two "views" of the
# same sample. In the default instance mode, NT-Xent pulls mean-pooled sequence
# embeddings together and pushes different samples apart in projection space.
#
# Key difference from MAE: discriminative (not reconstructive)
# Key difference from JEPA: global invariance (not local positional prediction)

name: contrastive

# Mode: "temporal" (per-timestep overlap pairs) or "instance" (mean-pool, SimCLR-style)
mode: instance

# Masking budget matches the core MAE/JEPA default; the two-view strategy is
# objective-specific.
mask_ratio: 0.5  # Experiment plan default (ablate over 0.3/0.5/0.75)

# Projection head parameters
proj_hidden_dim: 256   # Hidden dimension of projection MLP (4 * encoder d_model)
proj_output_dim: 64    # Output dimension (contrastive embedding space, = encoder d_model)

# NT-Xent temperature (0.07 suits instance-level with ~511 negatives at B=256)
temperature: 0.07

# Complementary masks: view 2 = ~view 1, ensuring zero overlap between views.
# Forces encoder to learn abstract semantics from non-overlapping windows.
complementary_masks: true

# @package task
# 24-hour mortality prediction (binary classification)
# Predict if patient will die within 24h after the observation window

task_name: mortality_24h
task_type: binary
n_classes: null
head_type: mlp
hidden_dims: [64]
dropout: 0.1
activation: relu
use_layer_norm: false
projection_dim: null

# @package task
# Remaining length of stay prediction (regression)
# Predict remaining days in ICU after the 24h observation window

task_name: los_remaining
task_type: regression
primary_metric: mae
n_classes: null
head_type: mlp
hidden_dims: [64]
dropout: 0.3
activation: relu
use_layer_norm: false
projection_dim: null

# @package task
# ICU mortality prediction (binary classification)
# Predict if patient will die during the remainder of the ICU stay after the 24h observation window

task_name: mortality
task_type: binary
primary_metric: auprc
n_classes: null
head_type: mlp
hidden_dims: [64]
dropout: 0.3
activation: relu
use_layer_norm: false
projection_dim: null
